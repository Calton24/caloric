# .eas/workflows/e2e-ios.yml
# EAS Workflow for iOS Simulator E2E testing with Maestro

name: E2E Tests (iOS Simulator)

on:
  # Disabled: preserving build allowance. Re-enable by restoring push/pull_request triggers.
  workflow_dispatch: {}

jobs:
  build_ios_simulator:
    name: Build iOS Simulator App
    type: build
    params:
      platform: ios
      profile: preview

  # run_maestro_tests is disabled — requires EAS paid plan.
  # To re-enable: subscribe at https://expo.dev/accounts/calton24/settings/billing
  # then uncomment the block below.
  #
  # run_maestro_tests:
  #   name: Run Maestro E2E Tests
  #   type: maestro
  #   needs: [build_ios_simulator]
  #   params:
  #     build_id: ${{ needs.build_ios_simulator.outputs.build_id }}
  #     flow_path: ./maestro/flows
  #     device_identifier: "iPhone 16 Plus"
  #     output_format: junit
